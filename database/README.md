# Database – EchoBoard

## Files
- `schema.sql`: MySQL schema for users, feedback, and products.
- `db-config.js`: Node.js MySQL connection file.

## Setup
1. Import `schema.sql` in MySQL Workbench or CLI.
2. Use `db-config.js` in backend to connect database.
